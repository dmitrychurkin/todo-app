export const lightTheme = {
  /**Specify your custom theme */
};

export const darkTheme = {
  /**Specify your custom theme */
};

export enum ThemingToken {
  Light = "light",
  Dark = "dark",
}

export enum LayoutToken {
  List,
  Grid,
}

export const dateTimeFormatting = "dddd, MMMM Do YYYY, h:mm:ss a";
